package gregtechCH.config;

public enum ConfigCategories_CH {
    general,
    loader,
    nbt,
    colour,
    fluid,
    optimize;
    public enum Machines {
        basic,
        boiler,
        generatorMotor,
        rotation;
    }

    public enum Reactors {
        adjustemission;
    }
}
